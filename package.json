{
  "name": "flatnmap",
  "version": "0.0.1",
  "description": "flattens the nested data structure",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rm -rf dist && mkdir dist && cp ./src/* ./dist/",
    "build": "npm run build:dist",
    "build:dist": "rm -rf dist && npm bin/babel src --out-dir dist",
    "test": "mocha --compilers js:babel-register --recursive"
  },
  "keywords": [
    "Normalized",
    "Data",
    "Flat"
  ],
  "author": "andrej bartko",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/webduvet/flatnmap"
  },
  "dependencies": {},
  "dev-dependencies": {
    "uglify-js": "~2.4.3"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-preset-es2015": "^6.22.0",
    "babel-register": "^6.22.0",
    "expect": "^1.20.2",
    "mocha": "^3.2.0"
  }
}
